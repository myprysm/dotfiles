# PROTOTYPE (wayfinder #7) — per-OS omz plugin lists (issue #4: plugin lists
# are template data; six WSL-dead plugins are live on the Mac and vice versa).
# Per-machine extras: add a `zshPluginsExtra` list to the machine-local
# chezmoi config [data] — appended by dot_zshrc.tmpl.
zsh:
  plugins:
    linux: [git, brew, bw, command-not-found, kubectl, helm]      # stub — final list at migration (#8)
    darwin: [git, brew, 1password, kubectl, helm, macos]           # stub — final list at migration (#13)
