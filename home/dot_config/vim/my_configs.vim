" Personal vim config. Managed by chezmoi.
" Lives outside ~/.vim_runtime: that path is an exact external, and a source
" entry sharing a target path with an external is an "inconsistent state" error
" that aborts the whole apply. Sourced from ~/.vimrc, after amix's basic.vim.
