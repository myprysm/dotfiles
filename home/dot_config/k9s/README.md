PROTOTYPE stub — k9s config. Linux target: `~/.config/k9s`. On macOS k9s reads
`~/Library/Application Support/k9s` instead — shared content will live in
`.chezmoitemplates/` and render to both targets, each shipped per-OS via
`.chezmoiignore`. Open point to react to (see ticket #7 comment).
