{{ if eq .chezmoi.os "linux" -}}
#!/bin/sh
# PROTOTYPE (wayfinder #7) — remaining apt prerequisites (issue #5 §4).
# bootstrap.sh already installed build-essential/curl/git/zsh before brew;
# this covers a repo cloned by hand without bootstrap.sh.
set -eu
sudo apt-get update
sudo apt-get install -y zsh git curl build-essential libffi-dev python3-dev
{{ end -}}
