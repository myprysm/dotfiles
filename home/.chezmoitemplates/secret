{{- /* PROTOTYPE (wayfinder #7) — per-domain secret router (issue #6).
       Usage: {{ template "secret" (dict "domain" "personal" "ref" "some-item" "field" "value") }}
       personal → Bitwarden (bw) · work → 1Password (op)
       Vault escape hatch: use the generic `output` function directly — not wired here. */ -}}
{{- if eq .domain "work" -}}
{{- onepasswordRead (printf "op://dotfiles/%s/%s" .ref .field) -}}
{{- else -}}
{{- index (bitwardenFields "item" .ref) .field "value" -}}
{{- end -}}
