# PROTOTYPE (wayfinder #7) — always a template. Patterns match TARGET paths.
# Per-machine escape hatch + zsh runtime artifacts (issue #4)
.zshrc.local
.zcompdump*

{{ if ne .chezmoi.os "darwin" -}}
# darwin-only targets
Library/**
.config/zsh/env.d/openblas.zsh
.config/zsh/env.d/gnu-tools-path.zsh
{{- end }}

{{ if ne .chezmoi.os "linux" -}}
# linux-only targets
.config/k9s/**
{{- end }}

{{ if not .bundles.ops -}}
.config/zsh/env.d/helm3-pin.zsh
{{- end }}

{{ if not .bundles.work -}}
.config/zsh/rc.d/50-sdkman.zsh
{{- end }}
