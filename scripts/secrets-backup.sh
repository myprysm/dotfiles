#!/bin/bash
# PROTOTYPE STUB (wayfinder #7) — monthly local vault backup (issue #11 §4).
# bw export + per-item attachment downloads (attachments are NOT in bw export —
# verified limitation), bundled into one passphrase-encrypted archive (age or
# gpg symmetric) in a machine-local directory. Never in this repo.
set -eu
echo "TODO (#11 §4)"
exit 1
